# agents

This directory holds user-facing agent configurations, orchestration logic, and
agent-specific workflows.

`truth-kit` is intentionally toolkit-first, so agent logic should remain a thin
layer over methods, tools, and workflows.

Subdirectories:

- `single-agent/`
- `multi-agent/`
