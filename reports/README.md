# reports

This directory holds worked investigations, failure analyses, and benchmark
reports.

The goal is to make methods and outputs inspectable rather than leaving them as
unverifiable claims.

Subdirectories:

- `worked-examples/`
- `failure-analyses/`
- `benchmark-reports/`
