# benchmarks

This directory holds datasets, task definitions, scoring logic, and regression
suites used to evaluate whether `truth-kit` actually improves truth-seeking
work.

Subdirectories:

- `datasets/`
- `tasks/`
- `scoring/`
- `regressions/`
